
Bu depo, C programlama dilinin temel yapı taşlarını ve kontrol mekanizmalarını öğrenme sürecimdeki **tüm** uygulama kodlarını barındırmaktadır. Her kod örneği, ilgili C kavramının mantığını pekiştirmek amacıyla tasarlanmıştır.


Aşağıdaki tabloda, klasördeki tüm dosyalar öğrenme akışına uygun olarak üç ana konuya ayrılmış ve her dosyanın amacı kısaca özetlenmiştir.


Bu bölüm, C programının temel yapısı, değişken tanımlama ve matematiksel/mantıksal işlemleri ele alır.

| Dosya Adı | Odak Konu | Açıklama |
| :--- | :--- | :--- |
| `DataType.c` | Veri Tipleri | C'deki temel veri tiplerini (`int`, `char`, `float`) tanımlama ve değişkenlerin kullanılması. |
| `Operators.c` | Temel Operatörler | Aritmetik operatörlerin (+, -, *, /) ve atama operatörünün kullanımı. |
| `TwoNumbers.c` | Giriş/Çıkış Pratiği | Kullanıcıdan iki sayı alıp bu sayılar üzerinde temel aritmetik işlemler yapmayı gösterir. |
| `operatorsSystem.c` | Gelişmiş Operatörler | Çeşitli operatörleri içeren ve kullanıcıya menü sunan daha kapsamlı bir uygulama. |

---

### 2. Karar Yapıları (`if-else`, `switch`)

Bu bölüm, programın akışını yönlendiren ve farklı koşullara göre kararlar almasını sağlayan yapıları inceler.

| Dosya Adı | Odak Konu | Açıklama |
| :--- | :--- | :--- |
| `PozitifNegatif.c` | Basit `if-else` | Bir sayının sıfır, pozitif veya negatif olma durumunu kontrol eder. |
| `TekCift.c` | Mod ve `if-else` | Mod operatörü (`%`) ile bir sayının tek mi çift mi olduğunu belirleme. |
| `AgeGroups.c` | `else if` Zinciri | Yaşa göre farklı gruplara (çocuk, genç, yetişkin) ayırma mantığının uygulaması. |
| `Weekdays.c` | `switch` veya `if-else` | Bir sayıya karşılık gelen haftanın gününü (`Pazartesi`, `Salı` vb.) ekrana yazar. |
| `Vote.c` | Basit Koşul Kontrolü | Yaş gibi bir kritere göre kişinin bir eyleme (oy verme vb.) yeterliliğini kontrol eder. |
| `GradeSystem.c` | Geçti/Kaldı Kararı | Öğrencinin notuna göre dersten başarılı olup olmadığına karar verir. |
| `GradeSystem2.c` | Harf Notu Sistemi | Detaylı not aralıklarına göre (A, B, C gibi) harf notu karşılığını bulan uygulama. |
| `score.c` | Puanlama Sistemi | Bir puanlama sistemini baz alan, koşullu ifadeler içeren uygulama. |

---

### 3. Döngüler ve Mini Proje Uygulamaları (`while`, `for`)

Bu bölüm, tekrarlayan işlemleri yöneten döngüleri ve bu döngüleri karar yapılarıyla birleştiren küçük projeleri içerir.

| Dosya Adı | Odak Konu | Açıklama |
| :--- | :--- | :--- |
| `sayilarisay.c` | Temel Sayma | 1'den kullanıcı girişine kadar ardışık sayıları yazdırma (döngü pratiği). |
| `sayilarisayN.c` | Geriye Sayma | Belirlenen bir sayıdan geriye doğru sayım yapma (döngü değişkenini azaltma). |
| `ID.c`, `SifreliGiris.c` | Şifreli Giriş Projesi | **while** döngüsü ve **if-else** kullanarak deneme hakkı olan şifre doğrulama sistemi. |
| `calculator.c` | Hesap Makinesi | Basit aritmetik işlemler yapan ve kullanıcı menüsü (`if/switch`) kullanan mini proje. |
| `calculator2.c` | Gelişmiş Hesaplama | Daha fazla işlem seçeneği veya hata kontrolü eklenmiş hesap makinesi. |

---

## 🙋‍♂️ Kişisel Profil

C programlama yolculuğumdaki bu çalışmaları incelediğiniz için teşekkür ederim. Öğrenmeye devam ediyorum ve projelerimi düzenli olarak güncelleyeceğim.


---
